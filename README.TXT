
Datalift code repository

The structure of the repo is to be defined (no trunk please).

do we do module/src etc.
or src/module etc.

I assume the first one with a build.xml at root?

Anyway, think before commiting ;-).

JeromE
