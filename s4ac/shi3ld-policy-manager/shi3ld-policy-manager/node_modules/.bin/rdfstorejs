#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../rdfstore/bin/rdfstorejs" "$@"
  ret=$?
else 
  node  "$basedir/../rdfstore/bin/rdfstorejs" "$@"
  ret=$?
fi
exit $ret
